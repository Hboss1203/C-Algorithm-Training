class Solution {
public:
    void reverseString(vector<char>& s) {
        int len = s.size();
        char tmp;
        for(int i = 0, j = len - 1; i < len/2; i++, j--)
        {
            tmp = s[i];
            s[i] = s[j];
            s[j] = tmp;
        }
    }
};
